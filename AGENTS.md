# 本地 Agent 说明

## 沟通

- 面向用户的叙述默认使用简体中文；代码、命令和技术标识保持英文。
- 先给影响与结论，再给行动、待决策和必要证据；没有对应内容就省略。
- 技术细节只保留对理解结论、判断风险或复现结果有帮助的部分。
- 明确区分已验证事实、推测和未解决问题。
- 用户建议不适合目标时，直接说明原因并提出更合适的方案，不迎合。

## 执行方式

- 根据用户目标和已授权范围持续完成工作。用户要求“先分析”“先展示方案”时，仅完成该阶段，不修改文件或执行后续操作。
- 已授权范围内的常规实现、修复和验证自主推进；已有明确授权的事项不重复确认。
- 缺失信息会实质改变结果时，提出具体问题；等待期间继续不依赖该答案的工作。常规实现细节结合项目现状合理决定。
- 用户在任务中补充要求时，结合原目标处理；除非用户明确取消或替换任务，否则回答插入问题后继续推进。
- 遵守上层指令和权限边界。用户明确要求优先于本文件的默认约定和技能指南。
- 文件或技能规则导致暂停时，指出具体文件、条款及适用原因，区分明确要求与自行推断。
- 不要因为假想风险，主动加警告、免责声明、审批流程或安全/合规清单。
- 复杂产品、架构和调试任务先明确用户目标、约束、关键不变量与验收标准，再确定实现路径；对外简述结论和必要取舍。

## 项目上下文与改动范围

- 开始修改前，阅读相关代码、项目说明及适用的目录级规则，理解现有实现和工作区变更。
- 优先复用现有架构、组件与工具；改动应直接服务于当前目标，避免无关重构。
- 保留用户已有修改，不覆盖或撤销来源不明的变更。发生直接冲突时，说明冲突并确认处理方式。
- 开发、启动、测试和构建命令以仓库现有文档、脚本与 CI 配置为准，不臆造命令。
- 不擅自提交、推送或发布；用户已明确授权的操作按授权范围执行。

## Prompt Prefix 与缓存

- 涉及模型请求构造、系统提示词、开发者指令、工具定义及顺序、历史消息组织或相关请求配置时，必须评估 prompt prefix 与缓存影响。
- 不涉及上述内容的改动可直接判定无影响，无需单独展开说明。
- 区分本文件对开发助手上下文的影响，与应用运行时发送给模型的 prompt prefix 影响。
- 优先保持稳定前缀；不得为缓存命中率牺牲正确性、消息可靠性和会话隔离。
- 确需改变前缀时，实施前说明具体变更与潜在影响，并取得用户确认；同一方案已获确认后不重复询问。

## 对话改动前置要求

- 涉及消息发送与保存、历史消息、上下文组织、流式输出、运行控制、失败恢复或会话隔离时，必须先完整阅读 `local_development_notes/CONVERSATION_FIRST_PRINCIPLES.md`。
- 同一任务中已完整阅读且文档未变化时，无需重复读取。
- 仅调整展示样式且不影响上述行为时，按 UI / UX 规范处理。
- 对话相关设计与实现必须以“消息可靠、过程连续、会话隔离、失败可恢复、用户可控制”为最高层判断标准。

## 失败处理与诊断

- 修复根本原因，不添加掩盖失败、伪造成功或未经设计替换执行路径的回退与静默降级逻辑。
- 按既定产品规则实施的重试、回滚和用户主动恢复不属于静默降级；必须保持状态可见、次数或范围有界，且不产生重复消息或重复操作。
- 失败时保留用户输入和已完成成果，提供可理解的原因与恢复路径；尚未查明根因时，明确说明已知事实。
- 新功能及关键流程变更应检查并补齐相关诊断日志，覆盖受影响的提交、启动、运行、完成和错误路径。
- 日志应支持定位任务、阶段与失败原因，避免记录密钥及不必要的敏感内容。

## UI / UX 原则

### 从用户任务出发

- 设计前先明确：用户是谁、当前要完成什么、已有多少背景知识，以及完成任务最大的阻碍是什么。
- 按用户完成任务的顺序组织界面，不按代码模块、内部架构或配置结构组织。
- 优先复用用户熟悉的交互方式；新增概念、步骤和选项必须有明确价值。
- 发生设计取舍时，优先考虑用户能否理解、完成任务并从错误中恢复。

### 让用户知道如何行动

- 用户进入页面后，应能快速理解这里能做什么、当前是什么状态、下一步如何操作。
- 突出当前任务最重要的操作，减少无关信息与不必要的选择；同一任务区域最多一个视觉主操作。
- 使用用户熟悉的简洁中文。必要时保留协议与技术专名，避免直接使用内部变量名和实现术语。
- 常用操作直接可达，低频选项按需展开；影响当前决策的状态、错误原因和恢复入口直接可见。
- 技术详情应便于阅读、选择和复制；原始参数、代码、日志与结果放在明确的详情视图中。

### 让操作结果清楚、可靠

- 操作后及时反馈结果；耗时任务展示可理解的进度或状态，并在适用时提供停止、取消或重试。
- 失败时说明发生了什么、哪些内容已保留，以及用户如何继续；不将未经整理的内部异常作为唯一反馈。
- 保留用户输入、已完成成果和有意义的页面状态，避免用户因失败、导航切换或重新打开应用而重复劳动。
- 切换项目或对话时隔离上下文；后台任务继续运行且状态可观察。
- 表单具有明确保存边界。保存失败保留输入；离开未保存内容使用统一确认流程。
- 对删除内容、改变历史或丢失未保存修改的操作，提前说明影响范围，优先提供可撤销能力。

### 降低阅读和操作负担

- 通过清晰排版、分组和适当留白帮助理解；不以紧凑、美观或信息密度牺牲可读性和操作便利。
- 相同概念和操作保持一致的名称、位置与行为。
- 持久浏览、管理和配置使用路由页面；局部选择使用浮层；简短原子流程或必要确认使用对话框。
- 同一功能不维护重复的页面与对话框流程。
- 布局适应小窗口、Windows 缩放和键盘操作，通过重排或滚动避免裁切与重叠。
- 按场景提供空、加载、成功、警告、错误、不可用、禁用和焦点状态；不得仅通过颜色表达状态。
- 空状态提供开始路径；加载、错误和不可用状态提供必要解释与下一步。

### 视觉与实现约束

- 默认采用明亮、克制、以任务为中心的视觉风格；具体表现服从用户主题配置。
- 优先复用 `core/theme.py` 的 `DesignTokens`、`ui/primitives.py` 及已有共享组件，统一颜色、字体、间距、密度和圆角。
- 优先通过排版、间距与细分隔线建立层次；卡片用于独立且重复的对象，避免无意义的容器嵌套。
- 克制使用紫蓝色强调色，避免渐变、玻璃效果、厚重阴影、粗描边、过度圆角和无交互价值的动效。
- 所有新增或调整的界面元素接入统一主题系统；AI 主题预览必须可隔离、可取消，不污染已保存配置。
- 使用控件类型、`objectName` 或属性限定 QSS 作用域，避免样式泄漏到后代控件。

### 用用户任务验收

- 使用代表性任务走完整流程，检查用户能否找到入口、理解信息、完成操作并确认结果。
- 根据改动范围验证首次使用、正常操作、出错恢复和中途离开的相关体验。
- 使用有针对性的测试与代表性截图检查受影响状态、小窗口、Windows 缩放及主题安全预览。
- 评价设计时优先看是否减少理解成本、无必要步骤和错误，再看视觉表现。

## 测试与验证

- 不要为可逆、影响小且只是复述实现的改动写测试。
- 跑与本次改动相称的测试，并完成必要检查。这些通过之后，只有出现新改动、新失败或尚未解决的疑点时，才扩大或重复测试；否则继续把任务做完。
- 缺陷修复应验证原始触发条件与受影响行为。
- 无法执行的检查必须说明原因与未覆盖范围，不得宣称通过。
- 行为变更时，同步更新受影响的用户文档与截图。
- 收尾删除本次任务产生、之后用不上的临时文件；仅清理确认由本次任务创建的内容，保留交付物及仍有用途的验证材料。
- 有文件变更时，提供一条中文标准提交信息。

## 版本与发布

- 当前应用版本以代码中的 `APP_VERSION` 定义为准，不在本文件重复维护版本值。
- 修改代码后，在任务结束时统一提醒用户确认是否需要更新 `APP_VERSION`；提醒不阻塞实现与验证，已有明确版本安排时不重复询问。

## 经验沉淀

- 完成非简单缺陷修复、启动或运行时诊断，或解决重复工具误用与调试绕行后，检查是否产生新增可复用经验。
- 有新增经验时，更新 `local_development_notes/PITFALLS_AND_METHODOLOGY.md`，记录现象、根本原因、修复方式、验证命令和预防规则。
- 优先更新已有相关条目，避免重复追加；已有内容完整覆盖时，无需为本次任务新增记录。
- 不将普通任务过程、临时输出或尚未验证的推测作为确定经验写入。