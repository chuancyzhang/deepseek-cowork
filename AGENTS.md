# 本地 Agent 说明

## 应用版本
- 当前应用版本：`APP_VERSION = "5.1.9"`

## 协作规则
- 每次修改代码后，提醒用户确认是否需要更新 `APP_VERSION`。
- 如果改动计划用于 GitHub Release，请保持 `APP_VERSION` 与发布标签一致。
- 修改文件或完成任务后，向用户提供一条中文的标准提交信息。
- 对于复杂的产品、架构或调试工作，在确定实现路径前先进行第一性原理思考。
- 编写代码时，不要添加回退或静默降级逻辑；应明确暴露失败并修复根本原因。
- 发布新功能时，提醒用户为关键的提交/启动/运行/完成/错误路径添加或确认诊断日志。
- 每次完成非简单缺陷修复、启动/运行时诊断、重复工具误用或调试绕行后，都要更新 `local_development_notes/PITFALLS_AND_METHODOLOGY.md`，记录可复用的经验。需记录现象、根本原因、修复方式、验证命令和预防规则，避免同类问题再次发生。
- 在开始需求分析或实现前，除纯 UI/UX 展示方向的需求外，必须判断拟议方案是否会改变对话前缀（prompt prefix），从而影响缓存命中率。优先采用不改变对话前缀的方案；如确实需要改变，必须先说明对缓存命中率的潜在影响，待用户确认后再实施。

## 对话改动前置要求
- 在分析或实现任何对话相关改动前，必须先完整阅读 `local_development_notes/CONVERSATION_FIRST_PRINCIPLES.md`。
- 对话相关改动必须以“消息可靠、过程连续、会话隔离、失败可恢复、用户可控制”为最高层判断标准。

## UI / UX 风格指南

### 视觉语言
- 整个应用统一使用明亮的 Linear 风格产品语言：紧凑、扁平、低干扰、以任务为中心，并克制地使用紫蓝色强调色。
- 保持较浅的信息层级。优先通过间距、排版和细分隔线建立层次，其次才考虑边框、卡片、阴影或装饰性容器；卡片仅用于彼此独立且重复出现的对象。
- 通过 `core/theme.py` 中的 `DesignTokens` 和 `ui/primitives.py` 中的共享基础组件，统一密度、间距、圆角、颜色、字体排版和语义状态。
- 避免渐变、玻璃效果、厚重阴影、粗描边、过度圆角、过饱和颜色，以及不具备交互价值的动效。

### 信息与交互
- 优先呈现当前任务、主要决策和下一步操作。将高级选项、技术细节、日志和原始数据放到渐进式披露层级中。
- 每个界面只设置一个视觉上占主导地位的主要操作。次要、破坏性和上下文相关操作应有明确区分，并靠近其作用对象。
- 使用主内容页面承载持久的浏览、管理和配置；使用浮层承载局部选择；仅将对话框用于简短的原子流程、破坏性操作、权限边界或未保存更改。
- 保持导航可预测，并保留相关页面状态。切换项目或对话时必须清除过期上下文；后台工作必须持续执行，并在导航切换后仍然可观察。
- 布局应适应可用内容宽度和缩放比例。优先保留主要内容和操作，通过响应式重排或滚动避免裁切和重叠。

### 内容与反馈
- 使用简洁、任务导向的中文文案和面向用户的术语。保留必要的协议名称，例如 `MCP`、`stdio`、`Streamable HTTP`、`JSON`、`Headers`、`Agent` 和 `Tool`。
- 在适用场景下，明确设计空、加载、成功、警告、错误、不可用、禁用和焦点状态。不得仅通过颜色传达含义。
- 对成功且可撤销的操作使用轻量反馈。失败时必须呈现根本原因和恢复路径，不得静默替换内容或降级处理。
- 技术内容应易于阅读、结构清晰、可选择并可复制；原始参数、代码、日志和结果应放在明确的技术详情视图中。

### 实现一致性
- 创建页面局部变体前，优先复用现有的页面标题、工具栏、导航行、数据行、表单行、通知、空状态、操作栏和其他共享基础组件。
- 修改任何 UI 时，必须保持全界面 AI 可配置主题与安全预览机制完整生效，所有新增或调整的界面元素都应接入统一主题系统，并确保预览变更可隔离、可取消且不会污染已保存配置。
- 将顶级目的地实现为路由页面，而不是大型阻塞式对话框。不要为同一功能同时维护重复的页面流程和对话框流程。
- 使用控件类型、`objectName` 或属性明确限定 QSS 选择器作用域，防止样式继承将边框、背景或颜色泄漏到后代控件。
- 保留有语义的脏状态和清晰的保存边界。保存失败时应保留用户输入，并让问题继续保持可操作、可见；离开未保存内容时统一使用一种确认流程。
- 使用有针对性的测试和代表性截图验证 UI 改动，包括相关状态、较小窗口和 Windows 缩放场景。确保面向用户的文档和截图与最终行为一致。

## Git 策略
- 本文件是项目协作上下文，应纳入 Git 版本管理。
- 修改本文件时，确保规则含义清晰，并与项目当前协作约定保持一致。
