# DeepSeek Cowork - 产品文档

项目团队：**deepseek-cowork team**。

## 1. 产品概述 (Product Overview)

### 1.1 背景
DeepSeek-V3.2 引入 **交错思维链 (Interleaved CoT)**，允许模型在思考阶段直接调用工具，避免“先想后做”的线性失真。DeepSeek Cowork 基于该能力构建桌面级智能代理，将推理与执行融入真实工作场景。

### 1.2 目标用户
面向需要高效率与可控自动化的专业用户与开发者，提供一款稳定、可扩展、可视化的桌面智能助手。

### 1.3 核心价值
*   **推理与工具融合**：思考过程中调用工具，降低幻觉与误操作。
*   **安全可控**：默认工作区沙箱，可按需开启 God Mode。
*   **技能可扩展**：`skills/` 与 `ai_skills/` 双轨制，支持热加载。
*   **自进化**：运行经验回写到 `SKILL.md`，持续提升成功率。

---

## 2. 功能特性 (Features)

### 2.1 交错思维链引擎
*   **Think-Call-Think**：模型先探索环境，再决定执行路径。
*   **低延迟推理**：减少冗余回合，提升响应效率。

### 2.2 桌面助手平台化
1.  **对话式文件操作**：批量整理、重命名、报告生成。
2.  **技能中心**：双目录技能管理与热加载。
3.  **工作区侧边栏**：文件树与内容预览一体化。
4.  **可视化监控**：聊天气泡、工具调用卡片、子任务状态。

### 2.3 企业 IM 与多入口控制
*   **飞书网关**：通过长连接接入 IM 指令。
*   **按日会话**：历史记录按日期分割存储。
*   **统一权限**：IM 与本地操作遵循同一工作区约束。

### 2.4 运行环境与依赖
*   **可执行文件**：直接运行，无需安装 Python。
*   **源码运行**：建议使用虚拟环境 **.venv\Scripts\python**。
*   **环境适配**：`env_utils` 在 Dev/EXE 模式下自动识别 Python 与 pip。

---

## 3. 用户使用流程 (User Flow)

1.  **配置与启动**
    *   设置 API Key 与 Provider（`openai` 或 `anthropic`）。
    *   选择工作区目录。

2.  **任务下达**
    *   用户：“帮我整理这个文件夹，把 2024 年报表移动到 Archive/2024。”

3.  **交错思维执行**
    *   **Think**：需要先列出目录文件。
    *   **Call**：`list_files(".")`
    *   **Observe**：返回文件列表。
    *   **Think**：规划移动与新目录创建。
    *   **Call**：执行工具或 Python 代码。

4.  **结果反馈**
    *   UI 中展示完成状态与详细过程。
    *   右侧工作区侧边栏同步变化。

---

## 4. 技术架构 (Technical Architecture)

*   **GUI**：PySide6
*   **LLM**：DeepSeek（OpenAI 兼容）与 Anthropic
*   **Pattern**：Interleaved Chain-of-Thought
*   **Core**：
    *   `Agent`：推理与工具调度
    *   `SkillManager`：技能加载与经验注入
    *   `Daemon`：无头推理服务
    *   `IM Gateway`：飞书消息接入
*   **Backend**：Python / PyInstaller

---

## 5. 竞品对比要点

| 维度 | DeepSeek Cowork | 传统 ReAct Agent |
| :--- | :--- | :--- |
| 思考中调用工具 | 支持 | 不支持 |
| 工作区安全边界 | 支持 | 依赖外部约束 |
| 桌面 UI 与可视化 | 内置 | 通常缺失 |
| 技能自进化 | 支持 | 通常缺失 |
