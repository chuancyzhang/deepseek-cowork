# DeepSeek Cowork 产品文档

当前应用版本：**4.9.5**

## 1. 产品定位

DeepSeek Cowork 是一个面向专业用户和开发者的 Windows 桌面 Agent 工作台。它把对话、项目工作区、工具调用、技能扩展、自动化和结果预览放进一个统一界面，目标是让 AI 不只“回答问题”，而是稳定地完成桌面任务。

## 2. 核心价值

- **推理与执行一体化**：模型可以边思考边调用工具，不需要把计划和执行拆成多套流程。
- **工作区可控**：本地文件能力默认围绕项目工作区展开，降低误操作范围。
- **能力可扩展**：通过 skills、bundled plugins、MCP 和自定义能力持续扩展。
- **过程可观察**：对话、工具调用、交付物、子 Agent、自动化状态都可以在 UI 里查看。
- **办公交付可预览**：从 AI 回复末尾生成报告、PPT 草稿、设计稿和文档型工作稿，结果文件可直接空降到右侧专注预览；基于 HTML 继续生成 PPTX、DOCX、PDF 时只锁定局部生成按钮，抽屉可继续使用或关闭。
- **模板化 PPT 输出**：从 HTML 工作稿生成 PPTX 时可指定模板，继承主题、母版、字号、色彩、版式节奏和顶部/底部图片。
- **知识可沉淀**：长期记忆和“沉淀为 Skill”让成功经验可以复用。

## 3. 主要功能

### 3.1 对话与项目

- 支持纯对话，也支持绑定项目的工作区对话
- 项目对话中的文件操作受工作区边界约束
- 运行中可以继续补充引导内容，而不必新开任务
- AI 回复末尾提供“生成办公稿”动作，可选择自由、PPT、设计稿、DOCX 输出类型；办公稿生成轮次默认收起为任务卡，交付物入口直接显示在卡片下方；从 HTML 继续生成 PPTX、DOCX、PDF 的对话过程也收纳到同类任务卡

### 3.2 桌面工作台

- 左侧项目与会话导航
- 右侧上下文抽屉，承载文件、交付物、任务观测和子 Agent 状态
- 文件与交付物抽屉采用浏览态/详情态：浏览态负责查找，详情态最大化预览与操作
- 交付物内嵌预览，覆盖 Markdown、HTML、图片、PDF、DOCX、PPTX、XLSX
- 从回复生成的办公稿会自动空降到右侧专注预览，便于边看边改
- PPTX 转换支持选择模板文件，并要求保留模板顶部和底部的品牌/装饰图片

### 3.3 技能与工具

- 内置技能位于 `skills/`
- 随包可选插件和用户技能位于 `ai_skills/`
- MCP 服务器可通过 `stdio` 或 Streamable HTTP 接入
- 腾讯文档、飞书文档、钉钉文档以三套独立可选 skill 内置，用户可在技能工作台填写平台凭据并调试对应脚本入口
- `tool` 是直接执行面，`skill` 是经验包

### 3.4 自动化

- 会话模板支持多步骤执行
- 定时任务支持计划管理和运行历史
- 人工确认步骤保留在聊天流中完成

### 3.5 记忆与沉淀

- Memory Center 维护长期摘要
- “沉淀为 Skill”把会话片段转成技能草稿
- 技能支持导入、导出、调试和热加载

## 4. 典型使用流程

1. 在设置中配置模型服务、默认工作区和需要的组件。
2. 新建纯对话，或将对话连接到某个项目。
3. 让 Agent 读取文件、生成代码、整理资料或执行自动化；需要报告、PPT、文档或设计稿时，在 AI 回复末尾点击“生成办公稿”并选择类型。
4. 在右侧抽屉浏览文件或下钻查看交付物；HTML 转 PPTX/DOCX/PDF 时可收起面板等待完成通知。
5. 把值得复用的经验保存到记忆或技能系统中。

## 5. 产品结构

- **设置中心**：模型、智能体、工作区、MCP、企业消息、更新、组件依赖
- **自动化中心**：任务模板、计划任务、运行历史
- **技能中心**：能力启停、导入导出、运行配置、校验与调试
- **会话界面**：主输入区、消息流、工具过程展示
- **上下文抽屉**：文件与运行观察

## 6. 非目标

当前产品重点不是：

- 做通用远程运维平台
- 把每个能力都变成固定协议
- 依赖外部 IDE 才能完成核心流程

Cowork 更强调桌面端的一体化工作流和可控执行体验。

## 7. 技术对应关系

- UI：PySide6
- 推理与调度：`core/agent.py`
- 后台执行：`core/daemon.py`
- 技能系统：`core/skill_manager.py`
- 自动化：`core/sop_manager.py`、`core/automation_manager.py`
- MCP：`core/mcp_client.py`
- 本地存储：`core/chat_storage.py`、`core/config_manager.py`

更详细的实现设计见 [TECHNICAL_DESIGN.md](TECHNICAL_DESIGN.md)，技能模型见 [SKILL_SYSTEM.md](SKILL_SYSTEM.md)。
