# DeepSeek Cowork 产品文档

当前应用版本：**5.0.1**

本文件描述 5.0.1 的产品基线；更早的发布范围、兼容性和验收清单见 [RELEASE_NOTES_5.0.0.md](RELEASE_NOTES_5.0.0.md)。

## 1. 产品定位

DeepSeek Cowork 是一个面向专业用户和开发者的 Windows 桌面 Agent 工作台。它把对话、项目工作区、工具调用、技能扩展、自动化和结果预览放进一个统一界面，目标是让 AI 不只“回答问题”，而是稳定地完成桌面任务。

## 2. 核心价值

- **推理与执行一体化**：模型可以边思考边调用工具，不需要把计划和执行拆成多套流程。
- **工作区可控**：独立聊天默认限制在应用数据目录内自动创建、且可在设置中修改根位置的聊天工作目录；项目聊天限制在所选项目工作区内，降低误操作范围。
- **能力可扩展**：通过 skills、bundled plugins、MCP 和自定义能力持续扩展。
- **过程可观察**：对话、工具调用、交付物、子 Agent、自动化状态都可以在 UI 里查看。
- **技术信息可读**：任务观测保留执行上下文、调用记录和技术详情三个一级视图，代码、参数、标准输出、错误和 Traceback 按内容类型呈现。
- **办公交付可预览**：从 AI 回复末尾生成报告、PPT 草稿、设计稿和文档型工作稿，办公流程的最新结果可直达右侧专注预览；普通工具生成文件只更新入口和列表，不打断当前阅读。基于 HTML 继续生成 PPTX、DOCX、PDF 时只锁定局部生成操作，抽屉可继续使用或关闭。
- **PPT Agent 工作流**：PPT Mode 提供统一入口，把主题、资料、模板、已有对话、修改需求和高视觉表达需求交给 PPT Agent；PPT Agent 默认生成演示文稿形态 HTML 工作稿，并按需选择 Guizang PPT Skill、Frontend Slides 或 Huashu Design 这类 html-ppt 策略。
- **模板化 PPT 输出**：从 HTML 工作稿生成 PPTX 时可指定模板，继承主题、母版、字号、色彩、版式节奏和顶部/底部图片。
- **知识可沉淀**：长期记忆和“沉淀为 Skill”让成功经验可以复用。

## 3. 主要功能

### 3.1 对话与项目

- 支持带独立聊天工作目录的聊天，也支持绑定项目的工作区聊天
- 模型选择按对话保存；OpenAI 兼容模型逐个保存 Chat Completions / Responses 协议，新建 GPT‑5.6 默认 Responses；同一对话每轮完成后可切换模型继续，运行中的一轮使用提交时冻结的模型快照
- 当前格式历史会话使用 SQLite 持久化；打开历史前先确认同会话保存队列落盘，加载失败时给出明确提示，不把失败误显示为空会话
- 项目和对话均支持归档；归档项目会从左侧栏和项目选择器隐藏，设置中心可查看并恢复已归档项目和对话
- 项目展开默认显示最近 5 个对话并保留“展开显示 / 收起全部”；项目、项目内对话和独立对话支持就地重命名，手动对话标题在后续保存和重启后保持不变
- 直接对话和项目对话中的文件操作都受各自工作区边界约束
- 运行中可以继续补充引导内容，而不必新开任务；没有引导时，同一轮的 reasoning、工具和阶段回复收纳在一个 AI 轮次容器中，阶段回复不提供消息级动作。每次引导结束当前轮次容器，并在真实发送位置显示为独立轻量行，之后的 reasoning 创建新的容器。引导先显示为等待安全节点，工具执行期间明确提示“完成当前步骤后应用”，真正进入下一安全节点后再原位更新为“已应用”
- 对话栏支持直接粘贴剪贴板图片并显示缩略图；图片保存在会话托管附件目录，只有配置了图片理解的模型才能提交，文本模型不会静默丢弃图片
- AI 默认直接执行任务；只有不澄清就无法可靠执行或可能选错对象/范围时，才用选项卡片询问关键问题，用户不响应时按推荐选项继续
- AI 回复末尾提供“生成办公稿”动作，可选择自由、PPT、设计稿、DOCX 输出类型；办公稿生成轮次默认收起为任务卡，交付物入口直接显示在卡片下方；从 HTML 继续生成 PPTX、DOCX、PDF 的对话过程也收纳到同类任务卡
- 新会话首页提供“PPT Agent”卡片，输入工具栏 Agent 选择器提供内置 PPT Agent 和自定义智能体入口；用户填写 PPT 需求、选择偏好并附加资料或 PPTX 模板，生成结果仍作为 HTML 交付物进入预览与转换链路

### 3.2 桌面工作台

- 左侧项目与会话导航采用浅色 Linear 风格的紧凑分组、单色图标和低干扰选中态，默认隐藏已归档项目和对话
- 右侧上下文抽屉默认隐藏，展开后以贴边平面承载文件、交付物、任务观测和子 Agent 状态
- 主对话区使用无卡片内容流和低饱和用户消息；同一 AI 轮次以一个外层容器承载按真实顺序交错的多段“深度思考 · 用时”、工具、阶段回复与最终回答。每段思考可独立展开，各阶段以紧凑留白和从标题起点开始的短细分隔线区分；空过程不绘制时间线、不预留高度。阶段回复无操作栏，最终回答保留复制、生成办公稿等动作；补充引导作为独立边界开启后续容器
- 能力、自动化和设置作为主内容区页面切换；返回后恢复原会话、输入焦点和右侧抽屉，一级导航不再打开大型模态窗口
- 文件与交付物抽屉采用浏览态/详情态：存在明确产物时以交付物为首页并按需进入工作区，没有产物时直接展示真实目录树；独立聊天或项目的真实空目录使用居中的轻量空状态，可打开对应目录并隐藏无意义控件，筛选无结果时仍保留恢复入口；文件类型和排序为两个独立控件，类型来自交付物注册表并显示数量，支持时间、名称和大小双向稳定排序
- 交付物内嵌预览，覆盖 Markdown、HTML、图片、PDF、DOCX、PPTX、XLSX
- HTML、Markdown 和文本详情使用明确的“预览 / 源码”切换；HTML 底部仅提供一个“生成文件…”入口，再选择 PPTX、DOCX 或 PDF
- 从回复或 PPT Agent 生成的办公稿会自动空降到右侧专注预览，便于边看边改
- 应用内消息、确认、输入和任务交互使用统一 Linear 弹层；普通反馈以右下角最多三条的可关闭 Toast 呈现，悬停时暂停消失
- PPTX 转换支持选择模板文件，并要求保留模板顶部和底部的品牌/装饰图片

### 3.3 技能与工具

- 内置技能位于 `skills/`
- 随包可选插件和用户技能位于 `ai_skills/`
- 支持安装符合标准的 Agent Skill 包，原始 `SKILL.md` 保留为权威说明，`skill.json` 仅作为本地检索、工作台和调试元数据
- MCP 服务器可通过 `stdio` 或 Streamable HTTP 接入
- 腾讯文档、飞书文档、钉钉文档、WeKnora、ShowDoc MCP、Airflow 与官方 Superset MCP 以独立可选 skill 内置。保存 Skill 配置会自动生成、更新并启用其托管 MCP，测试连接作为独立诊断动作。WeKnora、Airflow 复用隔离的 Skill Python 运行环境并按需启动 stdio；Superset 只连接运行在远端 Superset 环境中的官方 MCP 服务，并使用用户账号密码在内存中获取和刷新 JWT，不把 access/refresh token 写入 MCP 配置
- `tool` 是直接执行面，`skill` 是经验包
- 自动工具发现命中的技能全文只参与当前轮推理，不进入会话历史，避免临时技能上下文降低后续 prompt cache 命中稳定性
- `visualize` 是默认关闭的随包插件。启用后可通过受限 Python 计算生成 HTML Fragment，默认自包含，并可按固定白名单加载受信 CDN 的静态脚本、样式、字体或图片；发布后以按产物 CSP 隔离的沙箱 iframe 嵌入 AI 回复，运行时数据请求保持禁用，资源加载失败直接显示原因。关闭时不暴露工具、不创建新产物，已登记且哈希有效的历史产物只读可见

### 3.4 自动化

- 定时任务通过提示词描述目标，并可绑定引用能力或指定 Agent
- 定时任务支持计划管理、手动触发和运行历史
- 旧版 SOP 模板不再执行；迁移后的旧任务会停用保留，等待用户补充提示词后再启用

### 3.5 记忆与沉淀

- “设置 → 个性与记忆”统一维护回答个性、全局长期摘要和工作区摘要，并共享设置脏状态与失败回滚
- 会话工具菜单和 AI 回复末尾的“沉淀为 Skill”通过两步向导选择保存方式与会话片段，再生成可审阅草稿
- 技能支持导入、安装标准 Agent Skill、导出、调试和热加载

## 4. 典型使用流程

1. 在设置中配置模型服务、默认工作区和需要的组件。
2. 新建使用自动聊天工作目录的独立聊天，或将聊天连接到某个项目。
3. 让 Agent 读取文件、生成代码、整理资料或执行自动化；需要报告、PPT、文档或设计稿时，在 AI 回复末尾点击“生成办公稿”并选择类型。
4. 需要完整 PPT 工作流时，从首页卡片或输入工具栏“Agent → PPT Agent”打开内置 PPT Agent，填写需求、选择偏好、附加资料或 PPTX 模板，让它生成演示文稿 HTML 工作稿。
5. 在右侧抽屉浏览文件或下钻查看交付物；HTML 转 PPTX/DOCX/PDF 时可收起面板等待完成通知。
6. 把值得复用的经验保存到记忆或技能系统中。

## 5. 产品结构

- **设置中心**：模型、智能体、个性与记忆、工作区、MCP、企业消息、更新、组件依赖
- **自动化中心**：主内容区内的提示词任务、页面内编辑、Agent 绑定和运行历史
- **能力中心**：主内容区内的能力启停、导入导出、运行配置、校验与调试
- **会话界面**：主输入区、消息流、工具过程展示
- **Agent 选择器**：锚定输入工具栏，选择后立即加入当前输入；管理入口深链到设置
- **上下文抽屉**：文件与运行观察

## 6. 非目标

当前产品重点不是：

- 做通用远程运维平台
- 把每个能力都变成固定协议
- 依赖外部 IDE 才能完成核心流程

Cowork 更强调桌面端的一体化工作流和可控执行体验。

## 7. 技术对应关系

- UI：PySide6
- 推理与调度：`core/agent.py`
- 后台执行：`core/daemon.py`
- 技能系统：`core/skill_manager.py`
- 自动化：`core/automation_manager.py` 负责提示词、引用能力、Agent 绑定、定时调度和运行历史
- MCP：`core/mcp_client.py`
- 本地存储：`core/chat_storage.py`、`core/config_manager.py`

更详细的实现设计见 [TECHNICAL_DESIGN.md](TECHNICAL_DESIGN.md)，技能模型见 [SKILL_SYSTEM.md](SKILL_SYSTEM.md)。
