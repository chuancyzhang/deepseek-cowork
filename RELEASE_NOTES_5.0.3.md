# DeepSeek Cowork 5.0.3 发布说明

## 本次重点

5.0.3 增加了用户自定义主题基础设施。应用不新增官方亮色预设，默认浅色主题继续作为只读基线；用户可以自行组合字体、字号比例、密度、圆角、颜色和高级语义令牌，并把每款皮肤保存为独立 JSON。

## 外观与主题

- 设置中心新增“外观”页，可新建、复制、删除、导入、导出、预览和切换主题
- 用户主题保存在应用数据 `themes/` 文件夹中，一款皮肤对应一个 JSON
- 默认 Linear 主题只存在于代码中，保持只读，不生成、导出或接受 AI 修改
- 用户主题唯一 JSON 格式只含 `format`、`id`、`name`、`overrides`；`_state.json` 只记录当前自定义主题 ID
- 设置页编辑和主题选择只更新草稿；点击“预览”才一次性刷新，统一“保存”后正式落盘
- 预览后继续编辑会提示“当前预览不是最新”，不会防抖或实时刷新
- 主题目录的外部 JSON 变化会被运行时检测并重新应用
- 低文字对比度会显示建议，但不会阻止用户保存或切换
- JSON、令牌或系统字体错误不会静默替换，应用会显示原因并保留当前可用界面
- 临时弹窗关闭后会立即注销主题刷新绑定，不会因 Python 包装对象残留而阻断后续主题应用；若主题已保存但当前界面仍刷新失败，AI 主题和设置页都会保留持久化结果，并明确提示重启后载入
- 设置页保存了外观修改，或 AI 保存并启用了主题后，会建议重启应用，确保所有界面完整载入新主题；保存其他设置不会误提示重启

## AI 定制皮肤

内置 `theme-customizer` Skill 为 AI 提供受限工具：

- 按全局、左右侧栏、聊天、输入区、管理、反馈和预览外壳检查完整令牌与已安装字体
- 校验并规范化主题覆盖
- 发布不自动过期的临时预览，并按 `preview_id + revision` 增量修改
- 经用户确认后保存、启用或删除用户主题

AI 与设置页共用 `ThemeRepository` 和同一份令牌注册表。AI 不直接编辑应用 QSS，也不能绕过保存、启用或删除确认；预览修改后必须重新确认新的 revision。

## 诊断

主题预览、提交、应用、完成和错误路径写入应用数据目录下的 `theme_debug.log`。如果手工 JSON 损坏、包含未知令牌或引用未安装字体，界面会显示原始原因，日志可用于继续定位。

## 格式与边界

- 尚未发布，不提供 v1/v2、版本迁移或兼容逻辑
- 默认主题没有用户 JSON 文件，不能删除或覆盖
- 用户主题只保存覆盖项；未覆盖值从代码内默认主题解析
- 主题覆盖 Cowork 原生 UI 与嵌入内容的预览外壳，不改变交付物内容、系统标题栏或原生文件选择器
- 当前应用版本：`5.0.3`

## 建议验收

1. 在“设置 → 外观”新建主题并修改字体比例和强调色，确认界面没有刷新；点击“预览”后再确认一次性应用。
2. 放弃设置，确认界面恢复已保存主题。
3. 再次编辑并保存，重启应用，确认主题仍被启用。
4. 导出后重新导入主题，确认生成新的独立 JSON。
5. 让 AI 生成并增量调整主题，确认 revision 变化后旧 revision 无法保存，保存和启用时出现用户确认。
6. 制造低对比度配色，确认只显示提示；使用未知令牌或不存在的字体，确认明确报错。
