# DeepSeek Cowork 5.0.8 发布说明

## 本次重点

5.0.8 修复会话工作区入口沿用上一次浏览目录的问题。顶部项目名/“独立聊天”按钮和相邻的“文件与交付物”按钮现在始终以当前会话目录为准。

## 工作区与文件抽屉

- 打开文件抽屉前，对比当前会话目录、窗口目录和文件模型根目录
- 仅在目录不一致时重新挂载和刷新，目录一致时保留搜索、展开项与滚动位置
- 独立聊天和项目会话采用同一校准规则
- 当前目录为空、已删除或不可访问时，不再回退到上一次目录，并显示明确状态
- 项目侧栏浏览仍只改变全局浏览视图，不会改写已有会话的工作区归属

## 诊断

工作区入口的校准开始、完成和错误路径写入 UI 导航诊断日志，包含入口来源、会话 ID、旧目录和目标目录，便于定位状态串用。

## 版本与发布

- 当前应用版本：`5.0.8`
- GitHub Release 标签应为：`v5.0.8`

## 建议验收

1. 打开独立聊天 A，再从左侧浏览其他项目 B；点击顶部“独立聊天”或“文件与交付物”，确认展示 A 的聊天工作目录。
2. 打开项目会话 A，再浏览项目 B；从两个顶部入口打开文件抽屉，确认展示项目 A。
3. 在当前工作区展开目录并滚动，再次打开入口，确认目录未变化时状态仍保留。
4. 移动或删除当前工作区，确认界面显示“当前工作区不可用”，且不展示此前目录。
