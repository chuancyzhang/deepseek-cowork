# Source

- MCP package: https://www.npmjs.com/package/mcp-showdoc
- Repository: https://github.com/gongcf/mcp-showdoc
- Integration: bundled as a default-off Cowork AI Skill with an MCP server preset.

Cowork stores ShowDoc credentials through `config_fields`, then materializes them into the local MCP server configuration when the user clicks the preset generation action.
