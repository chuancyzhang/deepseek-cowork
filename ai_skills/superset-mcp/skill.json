{
  "version": 2,
  "name": "superset-mcp",
  "display_name": "Superset 数据分析",
  "kind": "knowledge",
  "capability_group": "optional-plugin",
  "source_type": "bundled_plugin",
  "source_format": "agent_skill",
  "default_enabled": false,
  "description": "Official Apache Superset MCP Streamable HTTP skill with username/password JWT authentication.",
  "description_cn": "官方 Apache Superset MCP 能力，通过用户名和密码获取 JWT，并以 Streamable HTTP 连接 Superset MCP。",
  "presentation": {
    "category": "data_analysis",
    "short_name": "Superset",
    "summary": "连接 Apache Superset，查询数据集、图表和仪表板。",
    "examples": ["查看仪表板", "查询数据集", "分析图表指标"],
    "access_note": "会连接你配置的 Superset 服务并访问授权数据；修改服务内容前仍需明确确认。"
  },
  "tags": ["superset", "apache superset", "mcp", "streamable http", "dashboard", "charts"],
  "triggers": ["Superset MCP", "Apache Superset", "Superset dashboard", "Superset chart", "Superset 数据集"],
  "anti_triggers": ["Airflow", "ShowDoc", "WeKnora", "third-party superset-mcp npm"],
  "tool_refs": [],
  "references": ["SOURCE.md"],
  "config_fields": [
    {
      "name": "SUPERSET_BASE_URL",
      "label": "Superset 地址",
      "kind": "text",
      "required": true,
      "env": "SUPERSET_BASE_URL",
      "placeholder": "https://superset.example.com",
      "help": "Superset Web/API 根地址，用于调用 /api/v1/security/login 和 refresh。"
    },
    {
      "name": "SUPERSET_MCP_URL",
      "label": "Superset MCP URL",
      "kind": "text",
      "required": true,
      "env": "SUPERSET_MCP_URL",
      "placeholder": "https://superset.example.com/mcp",
      "help": "Superset MCP Streamable HTTP endpoint，必须以 /mcp 结尾。"
    },
    {
      "name": "SUPERSET_USERNAME",
      "label": "用户名",
      "kind": "text",
      "required": true,
      "env": "SUPERSET_USERNAME",
      "placeholder": "admin"
    },
    {
      "name": "SUPERSET_PASSWORD",
      "label": "密码",
      "kind": "secret",
      "required": true,
      "env": "SUPERSET_PASSWORD",
      "placeholder": "Superset password"
    },
    {
      "name": "SUPERSET_PROVIDER",
      "label": "登录方式",
      "kind": "select",
      "required": true,
      "env": "SUPERSET_PROVIDER",
      "default": "db",
      "options": [
        {"value": "db", "label": "数据库账号 (db)"},
        {"value": "ldap", "label": "LDAP"}
      ]
    },
    {
      "name": "SUPERSET_MCP_TIMEOUT_SECONDS",
      "label": "超时秒数",
      "kind": "text",
      "required": false,
      "env": "SUPERSET_MCP_TIMEOUT_SECONDS",
      "default": "30",
      "placeholder": "30"
    }
  ],
  "mcp_server_presets": [
    {
      "id": "superset-mcp",
      "name": "Superset MCP",
      "description": "Official Apache Superset MCP Streamable HTTP endpoint with managed JWT authentication.",
      "enabled": true,
      "transport": "streamable_http",
      "timeout_seconds": "{{SUPERSET_MCP_TIMEOUT_SECONDS}}",
      "url": "{{SUPERSET_MCP_URL}}",
      "auth": {
        "type": "superset_password",
        "skill_name": "superset-mcp",
        "base_url_field": "SUPERSET_BASE_URL",
        "username_field": "SUPERSET_USERNAME",
        "password_field": "SUPERSET_PASSWORD",
        "provider_field": "SUPERSET_PROVIDER"
      }
    }
  ],
  "python_dependencies": [],
  "node_dependencies": [],
  "workflow": [
    "Use this skill for official Apache Superset MCP connections only.",
    "Saving the Superset URL, MCP URL, username, password, and provider automatically enables the managed MCP connection.",
    "Rely on Superset RBAC/JWT permissions and confirm high-impact actions with the user."
  ]
}
