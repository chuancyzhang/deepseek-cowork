# 数据安全插件源码验证记录

验证环境：2026-09-24，Windows 11，Python 3.14.2，项目 `.venv`。本次按用户要求不执行打包验证，产品验收由用户进行。没有提交、推送或发布。

## 已验证行为

- 新增单元测试 13 项：全部关闭与严格布尔默认值、关闭时无插件导入/存储、仅启用插件仍为空操作、协议文本范围、会话映射加密与隔离、关闭后还原、删除清理、策略冻结、取消、凭证提醒、已知字段还原、校验位、扫描边界、加载失败和事务性原文继续。
- 新增集成测试 7 项：Chat Completions、Responses、Anthropic 在 SDK 边界接入；附件展开后转换；系统及鉴权保持；整批工具未执行后从原文重生成；显示副本与协议历史分离；Skill 生成一次原文重试；关闭状态保持原有无效 JSON 参数行为；子 Agent 继承冻结策略和会话。
- 新增设置测试 4 项：默认关闭且不联动勾选、类别就地校验、保存重载、写入失败保留草稿并回滚、缺失插件页面可关闭、小窗口滚动可达。在 100% / 150% 缩放运行并检查截图。
- 相关回归：Agent bootstrap 14 项、执行授权 24 项、会话存储 29 项、远程 Skill 安装 12 项、默认模型设置 8 项、Anthropic Provider 4 项、对话渲染 17 项、记忆更新 13 项通过。

核心复现命令（仓库根目录）：

```powershell
.venv/Scripts/python.exe -m unittest discover -s test -p test_data_security.py -q
.venv/Scripts/python.exe -m unittest discover -s test -p test_data_security_integration.py -q
.venv/Scripts/python.exe -m unittest discover -s test -p test_data_security_ui.py -q
.venv/Scripts/python.exe scripts/benchmark_data_security.py
```

UI 测试设置 `QT_SCALE_FACTOR=1.5` 可复现缩放检查，设置 `COWORK_SECURITY_SCREENSHOTS` 为输出目录可保存截图。测试使用隔离的配置和合成数据，不调用真实模型。真实服务的端到端效果、应用打包后的资源加载及人工产品流程留给产品验收。

## 性能实测

本地 SDK 发送前的请求转换；引擎预热后 40 个不同文本样本，包含邮箱、手机号和普通文本。映射使用真实 Windows DPAPI；不计模型网络耗时、后台凭证扫描和异步日志写入耗时。提醒列统计的是请求线程增加的同步时间。

| 配置 | 100 KiB p95 | 1 MiB p95 |
| --- | ---: | ---: |
| 全部关闭 | 0.0002 ms | 0.0009 ms |
| 仅凭证提醒 | 0.1654 ms | 1.5843 ms |
| 邮箱、手机号令牌化 | 3.0102 ms | 24.4248 ms |
| 八类全部开启 | 8.8984 ms | 79.8416 ms |

以上样本达到同步提醒 ≤ 5 ms、令牌化 100 KiB ≤ 20 ms / 1 MiB ≤ 100 ms 的目标。八类开启时缓存命中的 1 MiB p95 为 1.1527 ms。关闭时结果为原请求对象，未加载引擎或创建存储；这里是插件入口微基准，不宣称测量了整个应用启动或网络链路。

## 既有失败与不覆盖范围

以下失败已在改动前源码复测，未因本次功能而修复或修改断言：

| 测试组 | 当前结果 | 既有问题 |
| --- | --- | --- |
| `test_deepseek_provider.py` | 70 项中 3 项失败 | 旧超时值及错误结果字段断言 |
| `test_llm_factory.py` | 10 项中 5 项失败 | SDK 构造函数增加 timeout 后的旧断言 |
| `test_skill_system_v2.py` | 40 项中 2 项失败、2 项错误 | 旧工具直接调用缺少现有授权/可用性上下文 |

对照方式：从 Git HEAD 读取相关原模块，在独立 Python 进程中替换模块命名空间后运行相同失败用例，不回退或覆盖工作区文件。DeepSeek 和模型工厂的既有失败已按用户指示不处理。

语法编译通过。文档链接检查通过；完整 `scripts/check_docs.py` 仍报告既有用户指南版本和截图数量不匹配（检查器要求 5.2.0 / 65 张，现有指南为 5.2.3 / 27 张），读取 HEAD 原指南复现相同结果，未更改无关版本或截图契约。

## 产品验收建议

1. 默认关闭发送普通消息、附件并运行工具，确认模型调用次数、授权和正常鉴权不受影响。
2. 单独开启一项，再开启令牌化并仅选邮箱，检查用户原文仍在、本轮提醒可理解，AI 新生成内容不被扫描。
3. 用客户表分析、邮箱查询、IP 诊断、代码与补丁任务检查稳定令牌及必要时的原文继续提示。
4. 切换会话、追加输入、停止和重试、关闭插件后重开历史，检查不串映射、不丢消息、不重复副作用。
5. 验收包内插件资源、正常加载、缺失/失败状态、主题预览及实际 Windows 缩放环境。
