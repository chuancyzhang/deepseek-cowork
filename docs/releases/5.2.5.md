# DeepSeek Cowork 5.2.5：可选连接、数据安全与运行恢复

按源码区间 `e0e2f2f..ce7e9ac` 补录，版本提交日期为 2026-09-24。本文不证明发行包已完成发布或真实服务验收。

## 用户变化

- 新增可选“账号与连接”，集中管理服务登录、用途与能力授权。原配置不自动迁移；任务保留启动时绑定的连接。
- 随包增加默认关闭的数据安全插件：可选技能和 MCP 风险检查、疑似凭证提醒及模型请求文本令牌化，各项功能独立开启。
- 停止对话时同步向受支持工具传递取消，保留已有输出和文件；修复断线重连阻塞与重复按钮撑宽。
- 修复乐享工具发现、登录身份解析、团队目录查询、游标分页、文件夹识别与多层浏览。
- 资料上下文按需加入，修复历史中的伪提问和重复回答；补充聊天记录下载说明。

## 兼容边界

统一连接认证失败不会自动换用旧凭据或其他账号；等待认证时保留消息与成果，停止或取消后迟到登录不能恢复操作。完整认证能力需要可选认证依赖，管理员分发与企业实网联调见[账号与连接](../account-connections.md)。

数据安全不等于全面外发控制，默认关闭。自动令牌化只处理支持范围中的用户与工具请求文本，AI 回复不做扫描脱敏；失败可能按设置可见地使用原文，详见[数据安全](../data-security.md)。

停止不撤销远端已完成操作；工具终止失败须明确检查剩余进程。

## 验收重点与已有记录

- 普通对话不因资料库登录自动引入资料；带引用会话重开后顺序正确、不重复提问。
- 验证乐享团队、多层目录、分页和选定范围读取；真实账号权限与外部服务仍需实网验收。
- 验证账号登录、等待认证、取消、切换会话与旧配置兼容。
- 分别验收安全插件关闭和开启状态，以及停止工具、断线重连和成果保留。

已有源码测试及未覆盖范围见[数据安全验证记录](../data-security-validation.md)和[账号连接验证边界](../account-connections.md#验证边界账号连接实现阶段记录)。这些历史记录不等于本次重新执行了功能测试或打包验收。
