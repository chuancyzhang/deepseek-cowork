---
name: connection-example
description: Read the status of the explicitly configured company service.
---

# Connection example

Use `connected_service_status` to read the configured service status. Configure
and authorize the service in Cowork's account and connections page first.

This sample is not installed or enabled by the application. Its host adapter is
deliberately limited to one read endpoint; no credential is exposed to the model.
